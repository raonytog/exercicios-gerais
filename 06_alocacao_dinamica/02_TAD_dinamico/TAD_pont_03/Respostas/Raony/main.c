#include <stdio.h>
#include "locadora.h"

int main () {
    char opcao[MAX_CARACTERES];

    while (scanf("%s", opcao) == 1) {
        if (opcao == "Cadastrar") {

        } else if (opcao == "Estoque") {


        } else if (opcao == "Alugar") {


        } else if (opcao == "Devolver") {

        }
    }
    return 0;
}